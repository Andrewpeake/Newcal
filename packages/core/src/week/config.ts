/** Days shown in week strip (Monday–Sunday). */
export const WEEK_VISIBLE_DAYS = 7;

/** ISO weekday: Monday = 1 … Sunday = 7 */
export const WEEK_START_ISO_WEEKDAY = 1;

/** Full 24h grid: midnight inclusive → next midnight exclusive → 1440 minutes. */
export const MINUTES_PER_DAY = 24 * 60;
